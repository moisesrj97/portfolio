---
import MetaHead from "@components/general/MetaHead.astro";
import Experience from "@components/home/Experience.astro";
import Hero from "@components/home/Hero.astro";
import { info } from "@data";
import Layout from "@layouts/Layout.astro";

const featureProjects = info.projects
	.filter((project) => project.isFeatured)
	.filter((_, index) => index < 6);
---

<!DOCTYPE html>
<html lang='en'>
  <head>
    <MetaHead
      title={'Portfolio'}
      description={info.about}
      ogImageUrl={'/assets/images/profile.png'}
    />
  </head>

  <body>
    <Layout>
      <Hero name={info.name} about={info.about} />
      <Experience title={'EXPERIENCE'} details={info.experience} />
      <Experience title={'EDUCATION'} details={info.education} />
      <section>
        <p class='text-black text-2xl w-full text-center dark:text-light mt-4'>
          👽 Psst! Don't leave without checking my <a
            class='text-black dark:text-light underline hover:text-blue'
            href='/talks'>last talks</a
          > and <a
            class='text-black dark:text-light underline hover:text-blue'
            href='/projects'>featured projects</a
          >!
        </p>
      </section>
    </Layout>
  </body>
</html>
